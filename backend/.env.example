# Exemplo de variáveis de ambiente para execução local do Backend Ktor
# Copie como referência e defina no processo local ou terminal (não commitar senhas reais)
DB_URL=jdbc:mysql://localhost:3306/lucroplus_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
DB_USER=root
DB_PASSWORD=SUA_SENHA_LOCAL
JWT_SECRET=CHAVE_ALEATORIA_COM_PELO_MENOS_32_CARACTERES
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
